on:
  push:
    branches: [ "main" ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ "main" ]

jobs:
  workflow-ci:
    runs-on: ubuntu-latest
    uses: calebtracey/devops/.github/workflows/goAPI.yml
