on:
  push:
    branches: [ "main" ]
  pull_request:
    # The branches below must be a subset of the branches above
    branches: [ "main" ]

jobs:
  workflow-ci:
    steps:
      uses: calebtracey/devops/ci/.github/workflows/goAPI.yml
